# ACTIVE WORK

BASE: `2bd943a1493359b1f1253cd108e492bb78960adf` (`main` Spotify-library workspace source checkpoint)
TASK: PL-001 — make PlaylistLab a usable Spotify playlist workspace while preserving guarded publish safety
TOUCHED: PlaylistLab/main.cpp; PlaylistLab/PlaylistModel.h; PlaylistLab/SpotifyClient.*; PlaylistLab/SpotifyImageCache.*; PlaylistLab/PlaylistLab.upp; tests/PlaylistCoreTest/*; README.md; docs/ACTIVE_WORK.md
STATUS: PlaylistLab now has a dark `upp_Ui` workspace built around three explicit concepts instead of the former blank target shell: (1) a visible Spotify playlist library, (2) the currently selected Spotify playlist with its actual tracks, and (3) a separate reference/working list. The library uses model-backed `UiList` + `UiItemRenderImage`, shows all playlists returned by `/me/playlists`, marks editable versus read-only playlists, remembers the last selected playlist/order mode, and automatically refreshes a previously authorized Spotify account on launch. Playlist artwork metadata is read from Spotify and image bytes are cached locally by stable playlist ID. A selected Spotify playlist can be copied directly into the reference list with `Use as Reference`, so CSV/paste are no longer the only reference sources. `Client ID...` is now explicit setup; playlist selection itself is visible in the library rather than hidden behind a `Spotify Target` modal. The selected playlist exposes an `Open Spotify` link-back. Existing deterministic preview/publish authority and no-delete behavior remain in place; read-only playlists can be inspected but cannot be published to.
PUBLISHED: guarded publish source `42a0f2ed70b3754863b493a1a3a71b90ad947a88`; verified guarded-publish docs HEAD `7b2df1769a4931ae4c971cad4847cce97ad1125b`; explicit main-window startup `b5a0ab2013da3989142cc1271a97ab600b1527f8`; SSL package support `38d3856d790568c723047bcb990c847bfe10cea4`; Spotify presentation metadata `818a2df8364cde11ef7461ed2920166dc034ed3c`; full-library interface `b127ee648e23d1c69bd2c286efb1ad07a238dfb1`; full-library/API metadata implementation `1a75a17ac9aa98063dd826f0f1ceb6e6a471f3d0`; artwork cache `4517ba6f2286b82628ffecb02042325cb22f6b41` / `9d2ff12bbddaabb18409b4ec0fc99549e2eecdd7`; image decoder/package wiring `7522e6c393b3d72c87e371053cf2c7517f7c97da`; dark visible Spotify workspace `2bd943a1493359b1f1253cd108e492bb78960adf`.
VALIDATION: VERIFIED historical guarded-publish baseline: at `7b2df1769a4931ae4c971cad4847cce97ad1125b`, Gary built/ran `tests/PlaylistCoreTest` under Windows U++/CLANGx64 with `88 checks completed`, and built/linked PlaylistLab as `GUI MT` with zero diagnostics. Interactive desktop review subsequently confirmed the real PlaylistLab window could be launched; that review exposed the former UI/product problem that the loaded target tracks and Spotify library were not visible. CURRENT source `2bd943a1493359b1f1253cd108e492bb78960adf` is NOT YET Windows-validated. The core test count remains expected 88 because no deterministic core tests were added or removed in this UI/library slice. New compile dependencies are `Core/SSL`, `plugin/jpg`, and `plugin/png`.
SAFETY: only `TRACK_EXACT` / `TRACK_AUTO` expose publishable Spotify identity. Review/Missing/Unresolved remain blocked. The publish worker executes the exact stored preview, re-reads target/snapshot before mutation, appends only missing reference occurrences, retains unrelated/unavailable target positions, never deletes/replaces items, verifies the exact post-add augmented target before reorder, uses evolving snapshots for reorder, treats failed mutation requests as potentially state-changing, and requires exact final readback. The new playlist browser adds `editable` metadata; read-only/followed playlists are visible and inspectable but the UI refuses publishing to them.
UX / STORAGE: Spotify auth/client ID remains in `playlistlab.spotify.json`. UI state (`last_playlist_id`, order mode) is stored separately in `playlistlab.ui.json`. Artwork bytes are cached under a `playlistlab-cache` directory beside the Spotify config and decoded through U++ raster plugins. Playlist artwork is shown through model presentation (`UiModelItem.image` / `UiItemRenderImage`), not one child control per row.
NEXT ACTION: Windows U++/CLANGx64 compile/run current `main`. First run `PlaylistCoreTest` (expected 88 actual checks), then build PlaylistLab `GUI MT` and launch it for Curt's visual review. Verify dark shell, visible playlist library, remembered/automatic Spotify refresh, playlist artwork appearing/caching, selecting playlists showing actual target tracks, `Use as Reference`, readable edit/read-only state, and that the former `Minimize(false)` bootstrap call is gone. Do not perform a live Spotify mutation unless Curt explicitly chooses a disposable playlist after inspecting the new UI. Fix only compile/runtime issues exposed by this pass; do not broaden into deletion/replacement or create-new-playlist workflows yet.
