# ACTIVE WORK

BASE: `c4655d8ccaf8202c5eb4f4fcc14795232f50130f` (`main` diagnostic checkpoint)
TASK: PL-001 — establish PlaylistLab architecture and first complete Spotify/CSV workflow
TOUCHED: PlaylistLab/main.cpp; PlaylistLab/PlaylistModel.*; PlaylistLab/PlaylistPlanner.*; PlaylistLab/PlaylistLab.upp; PlaylistLab/SpotifyAuth.*; PlaylistLab/SpotifyClient.*; tests/PlaylistCoreTest/*; tests/PlaylistGuiSmoke/*; README.md; GitHub.var; .gitignore; docs/ACTIVE_WORK.md
STATUS: PlaylistLab supports local CSV/clipboard import/export, document-authoritative drag reorder, asynchronous Spotify target loading, selected-track resolution, explicit candidate confirmation, Reference Slots / Reference First planning, deterministic publish preview, and an explicit preview-to-publish path. Publishing is available only from a fully publishable non-no-op preview with snapshot evidence and requires a second confirmation; no delete/replace workflow is implemented. The remaining acceptance blocker is isolated to GUI startup visibility in Gary's Windows validation environment.
PUBLISHED: bootstrap `9299c2d3297311b769ec64fda8f256f11d98c922`; core `67988831ed5b708f733fd09c974e901c083b5332`; safety `78c374b4f117f2168f824829cf6dc99394a85e30`; Spotify `57c6d35ee1bc8be4d204f63a8b3335bfa92a3968`; GUI shell `ab9fcd5430802706853e77e60aa62bf273c1469b`; Windows alignment `16d9930893f6eeb180187558572287293da293a5`; build cleanup `b77021d06ab4149a27583049db6f4e7e70696471`; deterministic preview `5d76083afede1d71c702534761c2804ac93a221b`; review hardening `c8e3bb70e44f2e5c30b575deb5bbad93aa675cf7`; interactive preview GUI `5e665389cc20d8e60cb2f7ebb5e062952e50bbaa`; state-gated identity `1fe99534cc0fbb1fa273737cfcddae3cba1507af`; canonical naming `d52cec1ffea327b1e324407360107c7ab423ed28`; guarded publish authority `9e5641df0c087c77db7e1441b352a98df371af48`; explicit publish GUI/tests `c886ee9a5f884e07d00846bffb32e719cd6d3844`; publish concurrency hardening `42a0f2ed70b3754863b493a1a3a71b90ad947a88`; publish docs `bb52beb84609333db328fe10ed2442d7ebc33371`; GUI startup smoke `c4655d8ccaf8202c5eb4f4fcc14795232f50130f`.
VALIDATION: VERIFIED at `7b2df1769a4931ae4c971cad4847cce97ad1125b`: Gary fetched current `main`, built and ran `tests/PlaylistCoreTest` under Windows U++/CLANGx64 with `88 checks completed`, and built/linked PlaylistLab as `GUI MT` with no compiler/linker diagnostics. The PlaylistLab process starts and remains responsive but exposed no targetable top-level window (`MainWindowHandle = 0`, blank title), so non-mutating GUI and guarded-publish interaction remain unverified. No Spotify credentials, disposable target or mutation were used. `tests/PlaylistGuiSmoke` is now published but not yet Windows-tested.
SAFETY: only `TRACK_EXACT` / `TRACK_AUTO` expose publishable Spotify identity; Review/Missing/Unresolved stay blocked even with stale URI data. The publish worker executes the exact stored preview rather than silently replanning. A stable target/snapshot read is required before mutation. Missing reference occurrences are appended only; unrelated and unavailable target positions are retained and no deletion is performed. After additions, PlaylistLab re-reads and requires the exact planned augmented target before any reorder. Reorders carry evolving snapshot evidence. Failed mutation requests are treated as potentially state-changing and trigger target recovery when possible. Success requires an exact post-write readback equal to the previewed desired URI vector.
NEXT ACTION: Windows-test `tests/PlaylistGuiSmoke` at or after `c4655d8ccaf8202c5eb4f4fcc14795232f50130f`. It uses only CtrlLib, the same `GUI MT` mode, explicitly calls `OpenMain()` before `Run()`, and writes `playlistlab-gui-smoke.txt` via `ConfigFile()` recording U++ `open` / `visible` state. If the smoke exposes a normal top-level window while PlaylistLab does not, make the smallest production bootstrap correction to explicitly open PlaylistLab as the main window. If the smoke also has no targetable window but reports `open=1` and `visible=1`, treat the blocker as validation-environment visibility rather than Spotify/UI logic and move manual GUI acceptance to an interactive desktop. Do not broaden into create-new-playlist or deletion workflows yet.
