# ACTIVE WORK

BASE: `42a0f2ed70b3754863b493a1a3a71b90ad947a88` (`main` guarded-publish source checkpoint)
TASK: PL-001 — establish PlaylistLab architecture and first complete Spotify/CSV workflow
TOUCHED: PlaylistLab/main.cpp; PlaylistLab/PlaylistModel.*; PlaylistLab/PlaylistPlanner.*; PlaylistLab/PlaylistLab.upp; PlaylistLab/SpotifyAuth.*; PlaylistLab/SpotifyClient.*; tests/PlaylistCoreTest/*; README.md; GitHub.var; .gitignore; docs/ACTIVE_WORK.md
STATUS: PlaylistLab supports local CSV/clipboard import/export, document-authoritative drag reorder, asynchronous Spotify target loading, selected-track resolution, explicit candidate confirmation, Reference Slots / Reference First planning, deterministic publish preview, and an explicit preview-to-publish path. Publishing is available only from a fully publishable non-no-op preview with snapshot evidence and requires a second confirmation; no delete/replace workflow is implemented. Core/build acceptance is green. Automated GUI interaction remains unavailable because the Windows validation environment loses visibility/targetability of U++ top-level windows even when U++ reports them open and visible.
PUBLISHED: bootstrap `9299c2d3297311b769ec64fda8f256f11d98c922`; core `67988831ed5b708f733fd09c974e901c083b5332`; safety `78c374b4f117f2168f824829cf6dc99394a85e30`; Spotify `57c6d35ee1bc8be4d204f63a8b3335bfa92a3968`; GUI shell `ab9fcd5430802706853e77e60aa62bf273c1469b`; Windows alignment `16d9930893f6eeb180187558572287293da293a5`; build cleanup `b77021d06ab4149a27583049db6f4e7e70696471`; deterministic preview `5d76083afede1d71c702534761c2804ac93a221b`; review hardening `c8e3bb70e44f2e5c30b575deb5bbad93aa675cf7`; interactive preview GUI `5e665389cc20d8e60cb2f7ebb5e062952e50bbaa`; state-gated identity `1fe99534cc0fbb1fa273737cfcddae3cba1507af`; canonical naming `d52cec1ffea327b1e324407360107c7ab423ed28`; guarded publish authority `9e5641df0c087c77db7e1441b352a98df371af48`; explicit publish GUI/tests `c886ee9a5f884e07d00846bffb32e719cd6d3844`; publish concurrency hardening `42a0f2ed70b3754863b493a1a3a71b90ad947a88`; publish docs `bb52beb84609333db328fe10ed2442d7ebc33371`. A temporary GUI startup smoke package was added at `c4655d8ccaf8202c5eb4f4fcc14795232f50130f`, validated, then removed after diagnosis.
VALIDATION: VERIFIED at `7b2df1769a4931ae4c971cad4847cce97ad1125b`: Gary fetched current `main`, built and ran `tests/PlaylistCoreTest` under Windows U++/CLANGx64 with `88 checks completed`, and built/linked PlaylistLab as `GUI MT` with zero compiler/linker diagnostics. PlaylistLab process startup remained responsive but the validation environment exposed no stable targetable top-level window. DIAGNOSTIC VERIFIED at `35d010dbbe8fd252f0c7392506ce3026cc165033`: the independent CtrlLib-only `PlaylistGuiSmoke` built/linked with zero diagnostics, process remained running, initially produced `MainWindowHandle = 4938020`, later exposed no stable handle/title through the validation layer, while its own U++ state file reported exactly `open=1` and `visible=1`. Therefore the missing targetable window is classified as a validation-environment visibility limitation, not a demonstrated PlaylistLab startup defect. No production bootstrap change is justified. Non-mutating GUI interaction and live guarded-publish behavior still require an interactive desktop. No Spotify credentials, disposable target or mutation have yet been used.
SAFETY: only `TRACK_EXACT` / `TRACK_AUTO` expose publishable Spotify identity; Review/Missing/Unresolved stay blocked even with stale URI data. The publish worker executes the exact stored preview rather than silently replanning. A stable target/snapshot read is required before mutation. Missing reference occurrences are appended only; unrelated and unavailable target positions are retained and no deletion is performed. After additions, PlaylistLab re-reads and requires the exact planned augmented target before any reorder. Reorders carry evolving snapshot evidence. Failed mutation requests are treated as potentially state-changing and trigger target recovery when possible. Success requires an exact post-write readback equal to the previewed desired URI vector.
NEXT ACTION: perform PlaylistLab's remaining GUI acceptance from a normal interactive Windows desktop rather than the non-window-targeting validation harness: inspect local import/export/reorder, target/resolve/review/preview gating, second publish confirmation and close-while-busy behavior. Only against an explicitly disposable Spotify playlist, verify the guarded add/reorder path and stale-target refusal. Do not treat lack of automation HWND visibility as a reason to alter production startup. Do not broaden into deletion/replace workflows yet; create-new-playlist/safe-copy work should remain a separate bounded follow-on after the guarded existing-target workflow receives interactive acceptance.
